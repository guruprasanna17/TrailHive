# TrailHive Landing Page

Beginner-friendly React project with the exact requested folder structure.

All pictures are inside:
src/assets/

Every section uses its own JSX and CSS file.

Run:
npm install
npm run dev
